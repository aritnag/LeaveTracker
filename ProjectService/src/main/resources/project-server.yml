# Spring properties
spring:
  application:
     name: project-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 8330   # HTTP (Tomcat) port