# Spring properties
spring:
  application:
     name: user-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 8999   # HTTP (Tomcat) port