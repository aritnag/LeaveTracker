# Spring properties
spring:
  application:
     name: role-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 8199   # HTTP (Tomcat) port