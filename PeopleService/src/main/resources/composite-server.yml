# Spring Properties
spring:
  application:
     name: composite-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 8299   # HTTP (Tomcat) port