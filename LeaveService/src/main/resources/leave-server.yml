# Spring properties
spring:
  application:
     name: leave-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 8444   # HTTP (Tomcat) port